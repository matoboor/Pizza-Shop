@if(Request.IsAuthenticated) {
        <text>Welcome <strong>@Html.ActionLink(@User.Identity.Name,"Detail","Users",null,null)</strong>!
    [ @Html.ActionLink("Log Off", "LogOff", "../Account") ]</text>
}
else {
    @:[ @Html.ActionLink("Log On", "LogOn", "Account") ]
}
