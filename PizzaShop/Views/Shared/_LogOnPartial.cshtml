@if(Request.IsAuthenticated) {
    <text>Vitajte <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink("Log Off", "LogOff", "Account") ] <br />
    </text>
}
else {
    @:[ @Html.ActionLink("Log In", "LogOn", "Account") ]
}
